---
name: sst-1496-week1-revisit
description: Revisit SST-1496 (Actual-track archetype-exhausted preview bug) before Week 1 of the NFL season wraps up
---

Revisit SurvivorPulse bug ticket SST-1496 in the "SP Stories & Tasks" Notion database (data source collection://35929ce5-833d-8156-9e29-000ba878443c, page id 3cd29ce5833d8130a544f9b7a9ea49e4, URL https://app.notion.com/p/3cd29ce5833d8130a544f9b7a9ea49e4) and decide whether it should now be built, ahead of the 2026 NFL Week 1 deadline the founder set.

BACKGROUND: SST-1496 is "BUG: Actual-track archetype-exhausted cell-kind never fires on real data — eliminatedAtWeek structurally blind to buyback exhaustion." It was triaged on 2026-08-31 by Pam (product) and left in Backlog at Medium priority as a non-blocking, silent-gap issue: a PRE-Apply preview cell on the "Actual" track (in client/src/components/cockpit/seasonGridCell.ts) that is supposed to warn a user their archetype strategy is about to run out of real buybacks, but structurally never fires because ForwardAdapter (server/services/strategyEngine/ForwardAdapter.ts, ~line 706 and ~line 722) hardcodes hasCompletedGame: false unconditionally, so the real loss/buyback-elimination branch in buildGreedyPath (shared/strategyEngine/greedyPath.ts) is unreachable from real Apply-generated data. It does NOT affect what Apply actually writes (that's the Proposed track, already correctly fixed and shipped in SST-1279) and does NOT affect real standing (resolveBuybackAwareStanding is untouched and correct). The real fix requires ForwardAdapter to report genuine historical game outcomes for completed weeks, likely blending with HistoricalAdapter — a backend/core-engine architectural change, not a quick patch. Full details, root cause, and file list are in the ticket's Description and in Pam's triage note (appended as page blocks near the bottom of the page, since Notion comments weren't available that session).

THE FOUNDER'S ASK: on 2026-08-31 the founder said to keep this in Backlog for now but wanted a check-in "sometime before the end of Week 1 of the season" to possibly address it. NFL Week 1 2026 is expected to run roughly Thursday 2026-09-10 through Monday Night Football on 2026-09-14 — verify the actual current-week dates against the live app (seasonInfoService / the games table) rather than trusting this estimate, since it was computed in advance without a confirmed schedule.

WHAT TO DO NOW:
1. Fetch the current state of SST-1496 from Notion (status, priority, any new comments/blocks) — don't assume nothing has changed since 2026-08-31.
2. Confirm via the SurvivorPulse app/DB where the season actually stands relative to Week 1 (has it started, is it about to end).
3. Re-assess: has anything changed that would justify prioritizing this now (e.g. does the approaching live season make an accurate PRE-Apply preview more valuable, has the founder said anything new elsewhere, has scope/effort changed)? Re-verify the ForwardAdapter hardcode is still present before relying on the ticket's claims.
4. If there's still no clear signal to build now, post a short signed status-check comment/note on the ticket (e.g. "[Claude] — Week 1 check-in: ...") explaining the call, and leave it in Backlog. If there IS a clear signal to build now, route to Ann (ann-the-analyst) to confirm AC/Test Cases against ForwardAdapter's current actual behavior, then to Felix (felix-the-forge) to build it TDD per this repo's standard testing rules (RED-proofed, real fixtures, call-site coverage), and report the outcome to the founder rather than deciding unilaterally to skip it.
5. Report back to the founder (in whatever session picks this up) with a short summary: current ticket state, season timing, and the decision made.