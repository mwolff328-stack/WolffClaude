# Claude Code Environment Cleanup — 2026-04-03

## Summary

Consolidated the Claude Code plugin/skill/agent ecosystem to eliminate duplication between the Everything Claude Code (ECC) plugin, user-level files, and the superpowers plugin.

## What Was Removed

| Category | Count | Details |
|----------|-------|---------|
| Duplicate commands | 49 files | ECC-copied commands in `~/.claude/commands/` (plugin provides them with `everything-claude-code:` prefix) |
| Duplicate agents | 19 files | ECC-copied agents not referenced by CLAUDE.md workflow |
| Stale skills | 28 dirs | Pre-plugin-era copies in `~/.claude/.agents/skills/` |
| Unused language rules | 6 dirs (30 files) | cpp, golang, kotlin, perl, php, swift |
| Vestigial marketplace | 1 entry | `finance-skills` from settings.json + cached dirs |
| Duplicate skill | 1 file | `cpo-daily-brief.md` (available via anthropic-skills plugin) |
| Old backups | 2 dirs | `backup-20260328-*`, `backup-20260402-*` |

## What Was Kept

| Category | Count | Details |
|----------|-------|---------|
| User-authored commands | 5 | bootstrap-feature, context-refresh, develop-feature, implement-slice, merge-ready |
| High-frequency shortcuts | 8 | tdd, code-review, plan, docs, e2e, python-review, save-session, resume-session |
| Workflow agents | 12 | architect, ba-analyst, build-runner, code-reviewer, doc-updater, e2e-runner, planner, prd-writer, qa-planner, refactor-cleaner, security-auditor, test-writer |
| Business domain agents | 9 | arlo-the-amplifier, deb-the-designer, felix-the-forge, hank-the-hawk, luigi, rex-the-relay, stan-the-scout, stu-the-scribe, vlad-the-verifier |
| Plugins | 3 | superpowers, everything-claude-code, github |
| Language rules | 3 dirs | common, python, typescript |
| User skills | 6 | neon-postgres, pre-deploy, + 4 learned skills |

## Before/After

| Metric | Before | After |
|--------|--------|-------|
| Commands in `~/.claude/commands/` | 62 | 13 |
| Agents in `~/.claude/agents/` | 40 | 21 |
| Skill dirs in `~/.claude/.agents/skills/` | 28 | 0 (deleted) |
| Rule directories | 10 | 4 |
| Registered marketplaces | 4 | 3 |

## Additional Changes

- Updated `rules/common/agents.md` to replace deleted agent references (tdd-guide -> test-writer, build-error-resolver removed, security-reviewer -> security-auditor, rust-reviewer removed)
- Updated `rules/common/development-workflow.md` to reference test-writer instead of tdd-guide
- Removed `finance-skills` from `extraKnownMarketplaces` in settings.json
- Initialized `~/.claude` as a git repo pushing to `mwolff328-stack/WolffClaude`
- Added `.gitignore` excluding sensitive files (settings.json with PAT), caches, and transient data
- Added PostToolUse hook for auto-commit on `~/.claude` changes
- Added CLAUDE.md section for WolffClaude repo and Notion sync conventions

## Issues Encountered

- Initial git commit included transient files (paste-cache, shell-snapshots, etc.) before `.gitignore` was updated. Fixed with a follow-up commit removing them from tracking.
- Backup had to use `/tmp/` instead of `~/.claude/backups/` because `cp -r` can't copy a directory into itself.
