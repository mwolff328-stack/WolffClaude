It's live — 200! But the response shape is different from what we expected. Let me grab the full payload and check all weeks to understand the real schema:

It's accessible but returning empty entries arrays — the demo pool has no picks recorded. The real API response shape also differs from what our code expects. Let me read the current API mapping code and update it to match the actual response:

The API is live but uses entries where our code expects allocation, and the demo pool has no picks recorded so entries is empty. I need to fix the field mapping and make the hook fall back to mock data when entries are empty. Let me also check the hook to ensure the fallback logic is correct:

Now I have a clear picture. Two things to fix:

api.ts — the real API returns entries but our code reads allocation; need to map from entries
The hook — when live data comes back with empty entries (no picks in demo pool), Path B should stay on mock data rather than showing blank weeks
Let me fix both:

Now let me restart the web app and quickly verify it loads correctly with the fix:

Landing page loads cleanly. Here's the full status after testing:

Endpoint    Status
GET /api/pools/:poolId    ✅ 200
GET /api/pools/:poolId/entries    ✅ 200
GET /api/games    ✅ 200
POST /api/pools/:poolId/portfolio/week    ✅ 200 — now unblocked!
The auth gate is gone. However, the demo pool (e6e25327...) has no recorded picks for any week — the API responds with "entries": [] for all 18 weeks. I've updated the code to correctly read the entries field (the real API uses that instead of allocation), and to gracefully fall back to mock data when entries are empty.

So right now Path B shows mock data correctly, and it's wired to automatically switch to live data the moment picks exist in that pool for the 2025 season.

Next step on the survivorpulse.com side: picks need to be entered into pool e6e25327-7755-4624-b9e8-7f9e67a974fd for the 2025 regular season weeks. Once that's done, the prototype will display real CMEA data end-to-end with no further code changes needed.