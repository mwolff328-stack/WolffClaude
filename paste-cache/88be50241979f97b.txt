Read phase8/artifacts/ca1.phase8.golden.json.

I need a partial threshold analysis. For every week in the 2025 season, identify the top-ownership team (highest pickPct in tables.weeklyFieldConcentration for that week).

Then for each week, calculate:
1. ownershipAtPickDeadline = that team's pickPct
2. singleWeekEliminationRate = (fieldSizeEnteringWeek - fieldSizeAfterWeek) / fieldSizeEnteringWeek
   Use chartSeries.remainingField for field sizes. Week N entering = remainingField[N-1], after = remainingField[N]. Week 1 entering = poolSize (1000).
3. gameResult = W or L for that team (from the fixture data or tables.independentPath result field)

Produce a table with ALL 18 weeks showing:
- Week
- Top team
- Ownership %
- Elimination rate %
- Game result (W or L)
- Ownership threshold met? (>= 40%)
- Elimination threshold met? (> 25%)
- Both thresholds met? (full event)
- Only ownership met? (ownership >= 40% but elimination <= 25%)
- Only elimination met? (elimination > 25% but ownership < 40%)

Sort by week ascending. Do not modify any files.