Review package.json, the existing Vitest configuration, and the current 
test directory structure before making any changes.

Task: Add test:e2e and test:regression scripts to package.json and create 
the minimum viable Vitest project configuration to support them.

Step 1 - Audit first:
- Review current vitest.config.ts (or vitest.config.js)
- Map existing test file locations and naming conventions
- Identify which existing tests would reasonably belong to e2e vs regression
- Do not move or rename any existing test files

Step 2 - Define the scripts in package.json:
- test:e2e → vitest run --project e2e
- test:regression → vitest run --project regression

Step 3 - Register the new Vitest projects in vitest.config.ts:
- Add 'e2e' project pointing to the correct test directory/pattern
- Add 'regression' project pointing to the correct test directory/pattern
- Match environment settings to existing project conventions
- Keep environment: 'node' consistent with current config
- Do not touch the unit or integration project definitions

Step 4 - Create placeholder test files if no existing tests qualify:
- One placeholder e2e test file (e.g. tests/e2e/placeholder.test.ts)
- One placeholder regression test file (e.g. tests/regression/placeholder.test.ts)
- Placeholder tests must pass (a simple assertTrue(true) equivalent)
- Include a comment in each: "Placeholder - replace with real tests"
- Do not invent assertions against application logic

Step 5 - Confirm:
- npm run test:e2e passes
- npm run test:regression passes
- Existing test:unit and test:integration scripts still pass
- No existing test files were modified

Flag any conflicts or ambiguities rather than guessing.