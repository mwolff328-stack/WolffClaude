Review the existing project structure, package.json scripts, and test 
configuration before making any changes.

Task: Implement a GitHub Actions workflow that runs the full test suite 
on every push to main, acting as a pre-publish gate.

Step 1 - Audit first:
- Identify all existing npm test scripts in package.json
- Note the test runner (Vitest) and any known configuration issues
- Check if .github/workflows/ directory exists

Step 2 - Create .github/workflows/pre-publish.yml with:
- Trigger: push to main branch
- Node.js version matching the project's current runtime
- npm ci for clean dependency install
- Sequential test execution (unit → integration → e2e → regression)
- Clear pass/fail output per test type
- Job fails fast on first test failure

Step 3 - Verify against known constraints:
- The Vitest config has a known deferred issue with environment: 'node' 
  blocking jsdom-dependent tests. Do not fix that issue here. Work around 
  it or explicitly skip affected tests with a comment flagging them for 
  the deferred workstream.
- Parallelism must be disabled where DB races are possible

Step 4 - Confirm:
- Workflow file is valid YAML
- All referenced npm scripts actually exist in package.json
- If a script doesn't exist, flag it rather than inventing it

Do not modify any test files or application code. Workflow file only.