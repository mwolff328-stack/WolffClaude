Read specs/active/004-ca1-reframe-chalk-collapse.md in full before writing any code.
Phase C objective: Add a reconstruction field to every HighEliminationWeek event. The reconstruction captures the pre-deadline information state for each event week.

Files to change:
FileActionphase1/contracts/response.dto.tsAdd HighEliminationWeekReconstruction type; add reconstruction field to HighEliminationWeekphase4/core/high-elimination-detection.tsBuild reconstruction inside detectHighEliminationWeeksphase4/tests/high-elimination-detection.test.tsAdd minimum 4 new testsphase6/artifacts/ca1.example.jsonRegeneratephase8/artifacts/ca1.phase8.golden.jsonRegenerate

Step 1 — Add type to phase1/contracts/response.dto.ts
Add after HighEliminationWeekChalkDetail:
typescriptexport interface HighEliminationWeekReconstructionPick {
  entryId: string;
  team: TeamKey;
  pickedIntoConcentration: boolean;
  winProbability: number;
  ownership: number;
}

export interface HighEliminationWeekReconstruction {
  week: number;
  topConcentrationTeam: TeamKey;
  ownershipAtPickDeadline: number;
  impliedWinProb: number;
  concentrationFlag: boolean; // true if ownershipAtPickDeadline >= 0.40
  independentPathPick: HighEliminationWeekReconstructionPick | null;
}
Add reconstruction: HighEliminationWeekReconstruction as a required field on HighEliminationWeek.

Step 2 — Build reconstruction in phase4/core/high-elimination-detection.ts
Constants (add at top of file):
typescriptconst CONCENTRATION_FLAG_THRESHOLD = 0.40; // matches CHALK_OWNERSHIP_THRESHOLD
After collapseType is determined and before assembling the event object, build the reconstruction:
typescript// Reconstruction: top concentration team is highest-ownership team regardless of result
const topConcentrationRow = allRows.reduce((best, r) =>
  r.pickPct > best.pickPct ? r : best,
);

const aliveIndependentPick = independentPathRows.find(
  (r) => (r.week as number) === week && r.alive,
);

const independentPathPick = aliveIndependentPick
  ? {
      entryId: aliveIndependentPick.entryId,
      team: aliveIndependentPick.team,
      pickedIntoConcentration: aliveIndependentPick.team === topConcentrationRow.team,
      winProbability: aliveIndependentPick.winProbability,
      ownership: aliveIndependentPick.ownership,
    }
  : null;

const reconstruction: HighEliminationWeekReconstruction = {
  week,
  topConcentrationTeam: topConcentrationRow.team,
  ownershipAtPickDeadline: topConcentrationRow.pickPct,
  impliedWinProb: topConcentrationRow.impliedWinProb,
  concentrationFlag: topConcentrationRow.pickPct >= CONCENTRATION_FLAG_THRESHOLD,
  independentPathPick,
};
Add reconstruction to the event object assembly.
Update imports at top of file to include HighEliminationWeekReconstruction.
Watch items before writing:

Confirm the exact field name for win probability on Phase1IndependentPathRow — it may be winProbability or winProb or something else. Read the type definition before writing.
Confirm the exact field name for ownership on Phase1IndependentPathRow — it may be ownership or pickPct. Read the type definition.
If multiple independent path entries are alive in the same week, use the first one found (do not error).


Step 3 — Add 4 tests to phase4/tests/high-elimination-detection.test.ts
Test 1 — Week 14 CHALK_CONCENTRATION reconstruction:

topConcentrationTeam === 'TB'
ownershipAtPickDeadline ≈ 0.5695
concentrationFlag === true
independentPathPick is non-null and pickedIntoConcentration === true (I2 picked TB)

Test 2 — FRAGMENTED event reconstruction:

concentrationFlag === false (top team < 40% ownership)
independentPathPick non-null, pickedIntoConcentration correctly set

Test 3 — null independentPathPick when no alive entry:

Build a fixture where no Phase1IndependentPathRow has alive === true for the event week
independentPathPick === null

Test 4 — topConcentrationTeam is highest-ownership team regardless of result:

Week with two teams: winner at 60% ownership, loser at 30% ownership
topConcentrationTeam must be the winner (60%), not the loser


Step 4 — Regenerate baselines
bashSKIP_DB_GUARD=1 node_modules/.bin/tsx scripts/regenerate-baselines.ts

Verification sequence:
bash# TypeScript check on changed files only
node_modules/.bin/tsc --noEmit 2>&1 | grep -E 'high-elimination|response\.dto'

# New tests only
NODE_ENV=test TEST_DISABLE_NETWORK=1 TEST_FAST_OPTIMIZER=1 node_modules/.bin/vitest run --config vitest.config.ts phase4/tests/high-elimination-detection.test.ts --reporter=verbose

# Full suite — must show >= 2108 passing
NODE_ENV=test TEST_DISABLE_NETWORK=1 TEST_FAST_OPTIMIZER=1 node_modules/.bin/vitest run --config vitest.config.ts 2>&1 | tail -8

# Spot check
node -e "const fs=require('fs');const a=JSON.parse(fs.readFileSync('phase6/artifacts/ca1.example.json','utf-8'));const w14=a.events?.find(e=>e.week===14);console.log('W14 reconstruction:',JSON.stringify(w14?.reconstruction,null,2));console.log('events with reconstruction:',a.events?.filter(e=>e.reconstruction).length);"

Invariants:

No DB imports in phase4/core/ or phase6/
detectHighEliminationWeeks remains a pure function
All 2108 currently passing tests must continue to pass
Byte-identical determinism maintained