---
name: project_survivorpulse_sst1344_rank_tie_display
description: "SST-1344 — Ready, Priority Medium, closed out 2026-08-12. Ranked list/Season Grid falsely numbered Rank-Score ties as a total order; fixed via a founder-approved shared-rank 'T-8' convention, confirmed with a real live-app measurement. Also the session's record of a Notion connector outage-then-recovery and a lesson on verifying subagent design estimates."
metadata: 
  node_type: memory
  type: project
  originSessionId: a343076d-b813-441a-b4c1-4b50ab96c6f4
  modified: 2026-08-12T20:50:31.293Z
---

SST-1344 ("Ranked list assigns distinct sequential ranks to teams tied on the same Rank Score") was spun off from Deb's design-triage comment on SST-1340 while filing that ticket. Root cause: `client/src/lib/entryRecommendations.ts`'s `assignRankedOrder` (line 437) always does `.forEach((t,i) => ranks.set(t.teamId, i+1))` — never shares a rank across a tie. Two call sites confirmed by Vlad (grep, no third surface): `PickGrid.tsx:2216` and `cockpit/seasonGridCell.ts:801`.

5-persona triage panel (Pam/Ann/Deb/Felix/Vlad, dispatched as real subagents per [[survivorpulse-bug-triage skill]]) converged unanimously: Severity Medium, Criticality High → Priority Medium. Two findings worth surfacing to whoever builds this:
- **Felix:** the `assignRankedOrder` `Map<teamId,number>` is NOT display-only — `PickGrid.tsx:2216` also uses it for the founder-locked "By Division" unified rank / default sort order. A naive fix that collapses tied entries to share one Map value would silently defeat `tests/strategyEngine/tieBreakMatchesRankedDisplay.sst1340.test.ts`'s TC-E (a `<` comparison proving SST-1340's engine/display tie-break agreement). Build the "T-8" shared-rank label as a presentation-layer derivation from the existing ordered Map, not by collapsing the Map itself.
- **Vlad:** `rankTieBreakParity.sst1183.test.ts` currently asserts a tied pair gets DISTINCT sequential ranks (`bal.rank=2, cin.rank=3`) as CORRECT — it certifies this exact defect rather than catching it. Will need revisiting when this ships. A live example of [[hollow fixtures / tests that encode the bug]] rather than the requirement.

Status set to **Blocked** (not the skill's default "Ready") because all 5 personas independently confirmed this can't go to dev until Deb completes a wireframe/spec pass and the founder approves the tie-display convention — a deliberate deviation from the skill's literal "triage → Ready" step, judged more honest than mechanically following it. Re-triage to Ready once that spec lands.

**Tooling incident, same session:** `mcp__notionApi__API-create-a-comment` failed 100% (3/3 attempts, all payload shapes) with `400 missing_version` — reproducing the exact defect the SurvivorPulse-repo-scoped memory `project_survivorpulse_notion_comment_outage_is_connector_specific` already documented from earlier the same day (2026-08-12, session 2993dd74). That memory's fix is "load the second connector, `mcp__d77c6777-2678-446f-b1ea-d56a8303dfb6__notion-*`, via ToolSearch and retry there." Tried exactly that here (`select:` with the exact tool names) — **it returned "No matching deferred tools found," i.e. that connector was not present in this session's tool roster at all**, unlike the session that wrote the memory. So the "switch connectors" fix is not a guaranteed fallback — connector availability appears to vary session-to-session (this session's cwd was `C:\Users\wolff`, a non-project directory, operating on repo files by absolute path, rather than a session rooted in the repo itself — that may be the actual variable, not random flakiness).

**How to apply:** when `API-create-a-comment` fails, still try the second connector (cheap to check), but do NOT assume it will be there. Have a real fallback ready: this session asked the founder directly (AskUserQuestion) rather than silently defaulting to the Notes-property workaround the older memory warns is "lossy" — founder chose "run the substantive work, defer only the signed-comment mechanics" (Notes property + full reasoning delivered in chat, comments to be posted once the connector's fixed). `API-patch-page` (property writes, incl. Notes) kept working the whole time — only comment-creation was down.

**Correction, same session, ~15 min later:** the "connector not present" conclusion above was too strong. On the NEXT turn (a follow-up founder instruction, no new session), `mcp__d77c6777-2678-446f-b1ea-d56a8303dfb6__notion-*` appeared UNPROMPTED in the deferred-tools system-reminder — I didn't even have to search for it. Loaded fine via `select:`, and both `notion-create-comment` and `notion-update-page` worked on the first try. Meanwhile `mcp__notionApi__API-patch-page`, which had been reliable all session, started returning `401 unauthorized` — confirming the OTHER memory's "later in the session every call began returning 401" pattern exactly. **Revised guidance: connector availability can change WITHIN a session, turn to turn — a miss on one ToolSearch call is not proof the connector is unavailable for the rest of the session. Re-check on a later turn rather than permanently writing it off, especially once the primary connector starts 401ing (that may be exactly the signal that the second one has come online).** Backfilled all 6 deferred comments (bug-filed + 5 persona verdicts + consensus) successfully once switched over — full audit trail is now real Notion comments, not just the Notes-field substitute.

**Notion property-write shape differs by connector**, confirmed in practice: `mcp__notionApi__API-patch-page` wants nested Notion property objects (`{"Status": {"select": {"name": "Grooming"}}}`); `mcp__d77c6777-...__notion-update-page` wants flat SQLite-style values (`{"Status": "Grooming"}`) under `command: "update_properties"`. Don't port a payload verbatim between them.

**Artifact verification gotcha:** Deb (dispatched as a subagent to do a wireframe/spec pass) published a real Claude artifact and returned its URL in her text response. Opening that URL in the Claude_Browser pane returned "Page not found" / a sign-in prompt — looked exactly like a hallucinated link. It wasn't: `Artifact({action: "list"})` showed it present and current under this account. The browser pane has no claude.ai login, so it can't render a private artifact regardless of whether the artifact is real — that failure mode is expected, not diagnostic. **To verify a subagent-reported artifact URL, use `Artifact({action: "list"})`, not an anonymous browser fetch.**

**Deb's spec pass (full detail in the artifact, https://claude.ai/code/artifact/40f7c2d5-524b-4e7d-95a7-0ccfeb752b46) — outcomes worth knowing without re-reading it:**
- Corrected a stale citation in the ticket itself: SST-1243's "136px cell / 110px content" Season Grid width figure is superseded by SST-1245 (2026-08-02), which widened `COL_WIDTH` to 164px (~136-138px content today). A design-triage pass that reads the current code, not just the ticket's paraphrase of it, caught a fact the ticket-filing pass got wrong by trusting an old comment.
- Found an UNREPORTED bug while checking whether "T-8" fits: the mobile Ranked-list rank span (`PickGrid.tsx:1242`) is a fixed 18px with no overflow handling, and is already razor-thin (0.7px spare) for TODAY's plain 2-digit ranks, let alone a tied label. Proposed fix: widen to ~30px, reusing the same flex-slack the row's Matchup column already has.
- Settled the "what counts as tied" question by citing the codebase's own SST-1241 precedent directly (a code comment reading "a second rounding implementation here would be SST-1241's defect class restaged") — ties must be judged on raw `rankedSortKey` equality, never the rounded displayed value, or this fix reintroduces the exact defect class that shipped once already.
- By actually reading the code rather than trusting Felix's triage note, found the rank Map has 3 real consumers, not the 1 Felix flagged: click-to-sort-by-Rank, "By Division" grouping, AND Season Grid/Week View. Design is additive (`assignDisplayRanks` alongside the untouched `assignRankedOrder`) so all 3 stay byte-identical.
- Flagged 3 things as genuinely open rather than guessing: the Season Grid hyphenated-vs-no-hyphen call (needs one real-browser measurement, same ritual this codebase has required twice before), aria-label copy (pending Sky), and a post-widening mobile tap-target check.

Status flow: Backlog → triage → **Blocked** (my call, all 5 personas said "needs spec + founder call") → founder said **Grooming** instead (more accurate Kanban semantics: it's actively being spec'd, not stalled) with Deb assigned → founder signed off on the mockup ("T-8 looks right, go with it") → **Ready** (final).

**Founder caught a real gap before sign-off: the "wireframe" wasn't visual.** Deb's artifact (linked above) turned out to be almost entirely her written spec, not the to-scale mockups she said she'd built — the founder's exact words were "I don't see the T-8 looks mockup." I had verified the artifact EXISTED (`Artifact list`) but never verified its CONTENT had real visuals, which is the actual gap: existence is not the same claim as "delivers what was promised." Built a real one myself directly from source (`shared/teamColors.ts` real hex per team, real inline styles copied from `PickGrid.tsx`/`seasonGridCell.ts`) and republished to the SAME artifact URL via `Artifact({url: <existing>})` — no dangling link, no link confusion for the founder. **Lesson: when a subagent's deliverable IS the artifact (a design/wireframe pass, not just research), verify the content matches what they said they built, not just that a URL resolves.**

**Local file:// preview in the Claude_Browser pane is a real, usable verification path for an artifact-bound HTML file before publishing** — `navigate` to a `file:///` path opens it in a **new tab** (does not reuse the current one; check `tabs_context` for the real tabId), and `get_page_text` against that tab returns full extracted text, confirming DOM structure/content landed correctly, even though `computer screenshot` failed in this environment ("Browser pane is not displayed, so the page is not compositing frames" — a pane-visibility issue, not a content issue). Worked cleanly the first time; a second attempt after an edit intermittently returned "no site is open" / stale tab state — re-opening a fresh tab (`tabs_create`) fixed it. Text-extraction verification (get_page_text / read_page) is more reliable than screenshot in this environment; don't block on the screenshot succeeding.

**The founder's own repo had the live deployed-dev URL the whole time** — `.replit`'s `DEV_BASE_URL` (a comment literally says "public URL of THIS workspace's dev preview"), not something to ask the founder for or guess at. Once on that real URL, a real measurement didn't need auth, real data, or a real tied-score scenario: `getComputedStyle(document.body).fontFamily` alone confirmed Inter is the resolved font stack, and a hidden off-screen `<span>` with the EXACT real CSS (font-size pulled from the actual `statLineStyle` source, `9.5px`) measured real candidate strings via `getBoundingClientRect().width` in one `javascript_tool` call. **This is much faster than trying to reconstruct a live 5-way-tie pool/entry scenario — for a pure text-width question, borrow the page's loaded font and measure in isolation; you don't need the feature's real data path at all.**

**The real measurement contradicted Deb's design-pass estimate by a wide margin** — she called the hyphenated `T-8` Season Grid string "~3-6px spare, right at the edge"; the real number was **38px spare**, comfortable. Her estimate combined two compounding errors: a substitute (non-Inter) font AND (per her own correction) the pre-SST-1245 136px-content figure re-applied inconsistently — she'd flagged the width was now ~136-138px but her worked numbers still landed close to the OLD constraint. Both errors pushed the same direction (making it look tighter than reality), so they reinforced each other instead of cancelling. **Lesson: a design agent's own stated uncertainty ("this is an estimate, needs live measurement") is not just a caveat to relay to the founder — it's a flag that the number itself may be significantly off, not just imprecise at the margin. Cheap to verify (~1 tool call here); worth doing before it becomes a founder-facing claim.**
