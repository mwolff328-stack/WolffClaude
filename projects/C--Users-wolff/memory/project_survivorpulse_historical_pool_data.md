---
name: project_survivorpulse_historical_pool_data
description: SST-784 unified Historical Pool Data onto shared PoolDataSubSection; QA preview-launcher branch-anchoring gotcha; scheduleType /api/games bug
metadata: 
  node_type: memory
  type: project
  originSessionId: 16c5a014-5f18-46ec-9790-14db6190d4c5
---

SST-784 (Done 2026-07-13, 2026-v1 commits 37106d2c / a52668de / 2e66b7c8): the left-nav Historical Pool Data page and the in-pool Pool Detail History tab are now BOTH backed by ONE shared component `client/src/components/PoolDataSubSection.tsx` (+ new `PoolPickAnalytics.tsx`). New thin left-nav wrapper = `client/src/pages/historical-pool-data.tsx` (Select Pool dropdown + shared component, keyed on poolId, inherits week/season from `useAppContext()`, no own selector). Route `/tools/historical-data` mounts it directly now.

Grooming lesson (in the story's 🎓 Learning): the "newer/polished" in-pool tab's Pick Analytics was a `coming soon` PLACEHOLDER, while the "older" standalone left-nav page (`data-management.tsx`) held the only REAL Pick Analytics. Verify which surface is actually more complete IN CODE before calling one the source of truth. Polish/recency ≠ completeness. Felix's code-read during grooming is what caught the inverted premise.

Both follow-ups now DONE (2026-07-14, 2026-v1), each through full /merge-ready + ephemeral Playwright CI:
- RETIREMENT: `data-management.tsx` DELETED. All in-app links rewired to the in-pool History tab `/pools/:poolId?analysisTab=historical`; legacy routes (/data-management, /pools/:poolId/data-management, /pools/:poolId/historical-data) kept as thin redirects. `appendSupportUserId` (client/src/lib/supportLink.ts) is the shared support-view deep-link helper (appends `&` when the path already has `?`; URL-encodes the id) — replaced 3 forked buildLink() copies. Lesson: the riskiest part of a link-rewiring retirement was a silent double-`?` URL bug (passes build, breaks admin support-view at runtime), and the reference sweep had to include the SERVER (a stale /data-management string survived in a routes.ts error payload the client grep missed).
- scheduleType BUG: fixed 2 live callers (data-management.tsx AND the undiscovered CurrentWeekPickAction.tsx = Pool Details Picks tab). Added canonical `deriveScheduleTypeFromWeek(week)` in shared/scheduleType.ts (week>18 => playoffs). Chose fix-callers over server-default because shared/scheduleType.ts mandates no defaults. UPDATE 2026-07-15 (Felix verify pass, SE-89/SST-842): NO live unfixed /api/games(/range) scheduleType call site remains — PoolPickAnalytics + picks-management fixed via deriveScheduleTypeFromWeek; remaining offenders (v2/pool-detail.tsx, PickEntryDrawer.tsx, dashboard.tsx, entry-dashboard-v2.tsx) are dead/unrouted V2 code. But the pattern has recurred 3 separate times, so any new games-endpoint work must make scheduleType structurally required in both URL and query key (see picks-management.gamesRangeScheduleType.test.tsx).

ENHANCEMENT DONE (2026-07-14): left-nav dropdown now SEASON-FILTERED by top-nav `selectedSeason` (season drives the pool list; selecting a pool no longer changes season — deleted only the `setSelectedSeason` line from the pool-switch effect, kept the week defaulting). DataCompletenessLegend WIRED to real status via existing `useHistoricalDataStatus` + new pure `computeOverallCompleteness(periods)` in shared/historicalDataStatusUtils.ts; renders on ALL THREE surfaces (left-nav, My Pools History tab via my-pools-detail.tsx, My Picks History tab via my-picks.tsx) because they all share PoolDataSubSection — activated its previously-ignored `pool` prop (needs poolType/startingWeek at every call site). Founder standing principle: Historical Pool Data is ONE shared component set, updated everywhere at once, never forked per surface.

E2E GOTCHA (new): after adding the season filter, `e2e/historical-pool-data.spec.ts` must switch the top-nav season to the fixture pool's season (2025) via the `season-selector-trigger` (shadcn Select, option name regex `/^2025( \(current\))?$/`, `.first()`) BEFORE opening the pool dropdown — otherwise the fixture pool (2025) isn't in the filtered list and the page shows the no-season-pools empty state (dropdown not rendered). Pattern borrowed from `e2e/portfolio-roi.spec.ts`'s switchSeason(). Also: an all-surfaces regression test must render the REAL call site (mount MyPoolsDetailPage, capture HistoryTab props) and be PROVEN to fail when the forwarded field is removed — a copied-fixture test passes silently while the real site is broken.

GOTCHA (QA/preview): the in-app preview launcher (launch.json) is anchored to the canonical repo path `C:\Users\wolff\Projects\SurvivorPulse`, which currently sits on branch `feat/design-system-foundation`, NOT 2026-v1. A browser smoke launched normally silently tests the WRONG branch. Detect via `@fs` paths in network requests; relaunch manually inside the correct 2026-v1 worktree. `C:\Users\wolff\Projects\spulse-merge-final` is a VALID 2026-v1 worktree (distinct from the stray clone warned about in [[project_survivorpulse_repo_path]]). Also: `@/components/ds` (EmptyState/LoadingState) exists ONLY on `feat/design-system-foundation`, not on 2026-v1 yet.
