---
name: project_survivorpulse_design_system
description: "SurvivorPulse UI design system — tokens exist, adoption stalled, new shared ds/ component layer + waved migration to fix app-wide inconsistency"
metadata: 
  node_type: memory
  type: project
  originSessionId: d6cc23be-fb75-4f39-9fa4-0f8a5ad7ff20
  modified: 2026-07-27T18:39:48.444Z
---

SurvivorPulse UI/UX consistency initiative (started 2026-07-06). The problem was NOT missing design tokens — Deb defined a full `--sp-*` token system back in April (see `docs/deb-design-tokens.md`: dark-native, Inter + JetBrains Mono, indigo-violet brand, IBM Carbon status; 189 files reference the tokens). The problem is **stalled adoption**: only `components/shell/` got migrated cleanly; ~79 pages still hand-roll UI. Audit found **42+ files with raw `<table>`** (only ~6 use shared `ui/table`) and headings mixing inline styles, `text-foreground`, and hardcoded `text-gray-900`.

**Founder direction (2026-07-06):** build the canonical shared component layer NOW, then migrate pages in reviewable waves. Build on the existing `--sp-*` system (no re-litigating visual language).

**Foundation built (Deb) on branch `feat/design-system-foundation` off 2026-v1, NOT pushed. Commits: a0dd0631 (foundation) + 89e8f829 (code-review fixes). Code-reviewed + all findings fixed (C1 mono-font-on-every-cell primitive bug, H1 sticky header, H2 dropped test-id, M1 table a11y scope/aria-sort/keyboard) — review-clean.**

**Founder approved proof page (admin/user-management) 2026-07-06 and greenlit Wave 1+ migrations to run overnight autonomously.** DataTable capability answers given: fills container width + horizontal-scrolls (no reflow) on mobile; zebra/sticky-header/sticky-left-col/numeric/loading/empty all built-in; sort affordance built-in but logic was caller-owned (gap). Wave work via sequential Deb agents on feat/design-system-foundation, committing atomically, NOT pushing; each wave code-reviewed after. PROGRESS: Wave 1 DONE — DataTable enhanced (clientSort asc→desc→none + `sortAccessor` nulls-last; `sticky:"left"|"right"`) commit eb5237b9; admin/auth-events 85c4eeb9, stripe-webhook-health 04682027, waitlist 1abe02ae; code-review found+FIXED a HIGH (desc reverse() flipped nulls to top) in 57220a12. Wave 2 DONE + code-reviewed (commits 749d0af3 data-import, 056a778f data-management, 16a4729e pools headings-only, 70a54d4c picks-management, 12c07eb5 analytics, 1a41541f analytics-dashboard, bfa60eb4 entry-analytics-dashboard). Review found: HIGH data-import lost initial default sort under clientSort; MEDIUM data-management sticky Week cell overpaints row highlight; pools list table deferred (needs expandable rows). FOUNDATION ENHANCEMENT ROUND: DataTable gained `defaultSort` (initial clientSort state), `renderSubRow` (caller-controlled expandable/detail sub-rows), `--sp-sticky-cell-bg` override (row-state-aware sticky bg) — commit 553958a4. data-import defaultSort fix + import cleanups — commit 337b9092 (a Deb agent hit a transient 529 mid-round; I recovered its complete-but-uncommitted data-import edit, verified, committed). Enhancement 553958a4 code-reviewed = PASS (correct, crash left nothing half-done) with ONE HIGH: the entire ds/ foundation has NO tests (no DataTable.test.tsx). TODO before/with Wave 3: add focused DataTable.test.tsx (defaultSort asc→desc→none from seed; renderSubRow truthy-only + correct colSpan; onSortChange precedence over clientSort; zebra parity w/ subrows) — run TARGETED single-file vitest (--no-file-parallelism, ~6GB heap) since full suite OOMs. Foundation-enhancement round CLOSED: pools.tsx renderSubRow migration 78230ac3 (reviewed PASS — first live renderSubRow consumer, faithful), data-management sticky fix 2b21aa03 (reviewed PASS). DataTable now has unit tests: tests/dataTable.test.tsx 11/11 passing (targeted run `NODE_OPTIONS=--max-old-space-size=6144 npx vitest run tests/dataTable.test.tsx --no-file-parallelism`), commit 7571d7ac — closes the no-tests HIGH. FOUNDATION FULLY LOCKED (built→reviewed→tested). ~19 commits on feat/design-system-foundation, still NOT pushed.

WAVE 3 (shared pool/entry components) — highest risk (reused across pages; some wrap pick-submission/money). POLICY for overnight autonomous: migrate ONLY clean read-only display tables; SKIP+FLAG anything interactive/pick-submission/money-sensitive/grid-shaped(matrix)/structurally-incompatible for a "needs human + Felix + Vlad review" list rather than forcing blind (no live smoke possible tonight — screenshots wedged). Wave 3a DONE: pool/HistoryPoolData migrated (d3efb3a4, read-only, verified); GameTable (rowSpan row-pair structure — DataTable has no multi-row-per-record model + admin mutations), PickHistoryTable (inline pick-edit mutation), AliveEntriesSummaryTable (edit mode + elimination-integrity math SST-656/657/658) all SKIPPED+FLAGGED for human/Felix/Vlad. Conclusion: clean read-only table migrations are now EXHAUSTED; everything remaining is interactive/pick-submission/money/grid needing live smoke (impossible while screenshots wedged) + Felix/Vlad → autonomous migration STOPPED at this risk boundary. Overnight run complete: 20 commits on feat/design-system-foundation, my footprint = 25 files all in client/+docs/+tests/ (three-dot diff), tree otherwise clean. BRANCH IS 11 COMMITS BEHIND 2026-v1 (which advanced with pool-rules-engine work) → needs rebase/merge onto latest 2026-v1 + re-verify before merging; my changes are client-only so expected low-conflict. Untracked shared/poolRules/ in the working tree = 2026-v1's pool-rules work leaking via the concurrent cleanup session (NOT mine — do not commit/delete). GameTable row-pair need = a real (but narrow) DataTable gap; deliberately NOT added speculatively.

**RECONCILED + WAVE-SCOPED 2026-07-15.** DS branch reconciled with 2026-v1 (merge fd14f41b, 21 ahead / 0 behind origin/2026-v1, green: typecheck + 47 targeted tests + V1 build + ephemeral Playwright CI). NOT merged to 2026-v1 (26fcf4a2) — founder-gated. Canonical repo C:\Users\wolff\Projects\SurvivorPulse now on 2026-v1; DS lives in worktree C:\Users\wolff\Projects\sp-wt-design-system. data-management.tsx RETIRED (SST-786) — DS migration discarded, do not resurrect. Verified inventory: 36 files still hand-roll <table>; EXCLUDE pages/v2/pools.tsx (frozen V2/410) + the strategy test file = 34 to migrate. WAVE PLAN (risk-ascending): A=backtester 4 read-only (low); B=7 small read-only displays incl. money EntryROITable/PortfolioROIDrawer (low-med, verify values); C=9 large analytics+strategy displays (med, SE-85/Make-Picks-adjacent → time after Make Picks; watch the cell-callback-has-no-row-locals gotcha); D=Historical Pool Data 3 comps (PoolDataSubSection/AliveEntriesSummaryTable/TeamPickDataEntry, host historical-pool-data.tsx) = CRITICAL, BLOCKED on a DataTable "bare mode" prerequisite (SST-809 deliberately removed sticky headers AND nested vertical scroll; DataTable forces overflow-auto via ui/table + opt-in stickyHeader → naive migrate regresses it) + must re-measure 375px/1280px via dev-auto-login recipe + Vlad review; E=7 interactive/pick-submission tables (high, defer to post-Make-Picks). GRIDS (PickGrid 1843L, AllocationGrid, HistoryPickGrid, RoadmapGrid) = OUT of DataTable scope (matrix not row-per-record) — recommend explicit exclusion. Routed to Pam to groom into epic + wave stories; open architecture decisions for Felix/Deb: (1) build DataTable bare-mode vs leave Historical Pool Data hand-rolled, (2) grids in/out. DEFER to human review: EntryROITable (money), pool/PickHistoryTable (1359L), pool/OverviewEntryRosterTable (1474L), pool/PoolStatsTab (693L), and grids pool/HistoryPickGrid + pool/AllocationGrid + pool/TargetsZone. Waves 4 (strategy/entry-analytics) + 5 (backtester) also deferred to groom-with-Felix/Vlad per original plan. NOTE transient API 529s are happening — agents may die mid-work; check git log+status to recover committed vs uncommitted before re-dispatching. Wave 3 next (pool/entry SHARED components — highest risk; consumed by multiple pages; Felix/Vlad co-review advised per original plan). Deb instructed to SKIP+report any page needing an unbuilt capability rather than force a lossy swap. Verify per slice with `npx tsc --noEmit -p .` + `npm run check` + `npx vite build` (NOT vitest — OOMs; NOT `npm run build` — Windows path bug). GOTCHA: a concurrent local cleanup session shares this checkout and switched the branch to 2026-v1 mid-work once — re-assert `git checkout feat/design-system-foundation` before any commit/read of ds/ files.

**Screenshot infra note (2026-07-06):** on this Windows box, Chrome MCP + preview `Page.captureScreenshot` both hang (CDP wedged) even on a trivial static page — screenshots unobtainable until Chrome restart. Workaround used: extract live DOM + injected compiled CSS via preview_eval, POST to a tiny local node receiver, serve/SendUserFile as a self-contained HTML render.
- New canonical component library at `client/src/components/ds/`, barrel import `@/components/ds`: `PageLayout`, `PageHeader`, `DataTable` (the centerpiece — columns-config API, replaces the 42 hand-rolled tables), `Section`/`SectionHeading`, `Heading`/`Text`, `StatCard`, `EmptyState`, `LoadingState`.
- Standard doc: `docs/design-system-components.md` (always use these, never hand-roll a `<table>` or inline heading styles).
- Proof migration: `client/src/pages/admin/user-management.tsx`.
- Verified: `tsc` clean, `npm run check` guardrails pass, `npx vite build` succeeds. NOTE: `npm run build` (scripts/build-v1.js) fails on a pre-existing Windows/Git-Bash path bug (`execSync('./node_modules/.bin/vite build')`), unrelated — use `npx vite build` directly. Full vitest still OOMs (see [[project_survivorpulse_local_verification]]).

**Gotcha:** `this-week.tsx` no longer exists in canonical repo — retired/absorbed into `my-picks.tsx`; stray copies survive only in `.claude/worktrees/` (contamination risk, flagged for cleanup — see [[feedback_survivorpulse_parallel_agents_worktree]]).

**HANDED OFF 2026-07-27 — this initiative's origin session is CLOSED.** Verified state at handoff: DS branch NEVER merged; now 21 ahead / **416 BEHIND** origin/2026-v1 (the 07-15 reconcile is obsolete); 2026-v1 has ZERO `@/components/ds` consumers. SE-90 epic + wave stories (A/B Ready, C/D/E stubs, 3 open decisions) exist in Notion but are STALE inputs to re-baseline, not a current plan. Successor: founder started a dedicated "SurvivorPulse UI consistency and responsive readiness" session (beta-launch push: full inventory incl. Admin, fix-vs-keep triage, measured 375px responsive audit, groomed fix plan). Key framing given to it: SALVAGE the self-contained ds/ layer (DataTable+tests+docs — port fresh onto 2026-v1) rather than merge/rebase the stale branch; redo page adoption against today's pages; founder go required before any landing. Must sync with the in-flight "My Pools Revisions for Beta" session (SST-1043..1046 shipped 0a2773b0; uncommitted pools.tsx in canonical checkout; hands off pools.tsx/my-pools-detail.tsx/MyPoolsCard.tsx/pool-shared/* until it ships). Future sessions: defer to that successor session's memory (SurvivorPulse project scope) for anything newer than this note.
