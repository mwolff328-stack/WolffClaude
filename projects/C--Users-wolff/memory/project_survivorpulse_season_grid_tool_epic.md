---
name: project_survivorpulse_season_grid_tool_epic
description: "2026-v1 Season Grid Tool epic — founder-requested enhancements to the standalone /tools/season-grid matrix, scoping decisions + data-availability gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: 519fa8dc-f63b-4c35-9daa-b5f3b4110083
---

New epic **2026-v1 Season Grid Tool** (SP Epics, Notion) created 2026-07-17 for founder-requested enhancements to the standalone Season Grid tool at `/tools/season-grid` (the read-only 32×18 matrix shipped under SE-89 / [[project_survivorpulse_homepage_reimagine]] as SST-843). NOT the Game Plan's embedded season grid (different component/surface).

Founder's 5 asks: full-height grid (no internal vertical scrollbar), team color-coding (first col + all cells), taller cells to fit Rank + Win % + Field Exposure labeled values, "BYE" label in bye cells, legend for cell shading.

Founder rulings (2026-07-17, via clarifying questions):
- **Field exposure** = pick popularity. Show where data exists, "N/A" otherwise. GOTCHA: `pick_popularity` table only has **2026 Wk1** loaded (`load-2026-week1-popularity.ts`); weeks 2-18 = N/A. Reopens the Phase-1 claim boundary Curie kept OFF the grid — copy must stay factual ("Field %"), never "best pick".
- **Rank** = archetype-dependent: reflects the team's rank under the **currently selected strategy archetype** (safe_chalk / 80_20_blend / max_equity — the 3 fixed cockpit archetypes, archetypeCopy.ts). Needs an archetype selector + per-week ranking source + a footnote below the grid.
- **Cells**: show a **team icon with color coding** (not just text); **cell background tint = win% intensity** (retain existing --sp-shade-1..5 ramp). GOTCHA: no team logo assets exist in repo (TeamUsageDrawer logoUrl always falls back to abbrev); licensed NFL logos carry IP. So "team icon" = brand-color chip from `shared/teamColors.ts` with abbreviation — flagged as Open Question for founder before build.

Decomposition (2 stories, both V1 Pre-Launch under the new epic):
- **Story A (visual redesign, Deb-led, small Felix touch for exposure)**: full height, team color chips, taller 3-row cells (Rank slot = placeholder "—" until B), win% tint, BYE label, field exposure (real where exists / N/A), shading+state legend. Fastest to Ready.
- **Story B (archetype-aware Rank, Felix+Stan+Deb+Sky, Size L, depends on A)**: archetype selector + per-week rank under selected archetype (from canonical optimizer/derivedMetrics, no ad-hoc) + footnote.

Claim boundaries from SE-89 still bind: no Composite Score, no EV-lift, no profit/ROI-edge language anywhere on the grid.

STATUS 2026-07-17: epic Active, both stories Ready. Story A BUILT + pushed to 2026-v1 (commit `1d24e461`, "SST-903 Season Grid tool visual redesign", Deb). Verified: tsc + build clean, 112 targeted unit + 2 real-DB integration tests green (failing-first RED captured for full-height/BYE/Field% N/A). Field Exposure reused existing GET /api/pick-popularity (no games-range change). Gotcha found: teamColors.ts comment claiming all primaries take white text is WRONG (DEN/LAC/MIA fail AA vs white) — new colorContrast.ts picks per-chip text color. Then founder ruled: show Spread BELOW Field% — done (commit `30d3e2ed`, cells now 4-row: Rank/Win%/Field%/Spread). LIVE VISUAL SMOKE PASSED on real authed dev app (localhost, dev-auto-login, ep-flat-rice): full-height confirmed (scrollH==clientH, no inner scrollbar), team chips real brand colors w/ per-chip AA text (DEN/LAC/MIA dark text), Field Exposure works end-to-end vs real data (Wk1 LAC 29%/DET 22%; Wk2+ N/A), BYE/legend/holiday markers present, mobile 375px sticky + no interactivity. GOTCHA: the Browser screenshot tool TIMES OUT on the ~3.5k-px-tall grid (verify via DOM/computed-style/network instead of pixels). Minor non-blocking: sub-1% exposure rounds to "0%". Story A DONE pending Vlad manual on-device pass.

FOUNDER ITERATION 2 (2026-07-17, after screenshot): approved "<1%" over "0%"; approved Story B; + 6 more cell changes → new story **A.1 "Season Grid cell refinements"** (3a129ce5-833d-8162-8352-fb087534f35d, Ready): opponent as team-color chip too, MOVE Rank/Win%/Field% into a hover tooltip (visible cell = opponent chip + spread only), sticky first row+col, center chips/WK/BYE, <1% exposure. TENSION RESOLVED: sticky header needs a bounded scroll region, so A.1 re-bounds the grid to viewport height (internal scroll) — this SUPERSEDES SST-903's "no internal scrollbar". Deb building A.1 now.

STORY B ARCHITECTURE (orchestrator nailed it): rank = within-week rank of `scoreTeamForStrategy(winProb, projectedPickShare, 0, archetypeConfig, ctx)` from `shared/strategyEngine/pickScoring.ts` (THE canonical scorer — reuse, never re-implement). Archetype configs = `COCKPIT_ARCHETYPES` (`shared/strategyEngine/cockpitArchetypes.ts`, default 80_20_blend). CRITICAL: winProb + pickShare must come from the **ForwardAdapter projected** data (same as cockpit), NOT the pick_popularity table (Wk1-only → would make all 3 archetypes rank identically wk2-18). So Story B needs a backend endpoint (Felix) that runs ForwardAdapter + scoreTeamForStrategy per week. Rank fills the A.1 tooltip's Rank line. Sequencing: A.1 → then Story B (don't run 2 build agents in same worktree — cross-contamination).

A.1 DONE + pushed (commit `a3761284`, Deb). Verified: tsc/build clean, 69 targeted tests green, live DOM smoke confirms opponent color chips + spread-only visible cell + centering + IND row CLEAN (earlier garble = screenshot artifact). GOTCHA (new, important): the in-app preview browser reports **window.innerHeight=0**, so ANY `100vh`/`calc(100vh-...)` layout collapses to 0 in the preview and can't be visually verified there (real browsers fine). A.1 uses `max-height: calc(100vh - 180px)` for the sticky scroll region — the 180px chrome offset is a GUESS (unmeasured) and sticky/tooltip visual behavior still needs a REAL-browser pass (founder or Vlad). Also: agent WebFetch CANNOT reach the private Notion story page (redirect loop) — subagent briefs must be fully self-contained, don't rely on the Notion URL.

STORY B DONE: backend `b17c7a50` (Felix — pure shared/strategyEngine/seasonGridRank.ts + seasonGridRankService.ts + GET /api/season-grid/ranks) + frontend `70c86a03` (Deb — selector/tooltip-rank/footnote) + claim-fix `be5c569a`.

ALL 3 STORIES DONE on 2026-v1 (2026-07-18 overnight); epic left Active pending founder AM visual smoke. Gate results:
- In Review (code-reviewer agent) found ONE HIGH: reused cockpit tagline "Small Edge Off the Crowd" rendered "Edge" on the Season Grid selector (forbidden on the read-only claim surface) + the page-level claim test only matched "proven edge" so it shipped green. FIXED be5c569a (render clinical labels only; test now matches bare \bedge\b, RED-then-GREEN). LESSON: re-audit reused "already-governed" copy against the DESTINATION surface's boundary; guard tests must match the bare forbidden word not a phrase.
- e2e Playwright CI: 5/6 shards green; persistent shard-5 red = UNRELATED pre-existing my-picks perf timeout (UC-4.10 /my-picks?pool=&entry= >60s; slow cockpit 3.1s/strategy 3.8s/games-with-spreads/~10 pool picks). Filed BUG (3a129ce5-833d-81bd-be2b-e89273a52f2a). NOT Season-Grid-caused (ranks endpoint only fires on /tools/season-grid; 5/6 green rules out global breakage). Diagnosed via shard-5 WebServer logs.
- Pre-Publish Gate CI: infra-blocked (BackTesting-Prototype sibling-repo checkout fails → all test stages skipped); can't serve as a gate.
FOUNDER AM SMOKE (2026-07-18) FOUND A REAL BUG → FIXED (commit `0362c7ad`): grid rendered broken (WK header row collapsed into a vertical stack, team column missing, internal scrollbar present). ROOT CAUSE: `display: flex` was applied directly to `<th>`/`<td>` table cells (.season-grid-th-week, .season-grid-td-team, .season-grid-cell-bye) — flexing a table cell drops it from table layout and collapses the row. The 4 CSS tests had REQUIRED display:flex + the bounded max-height scroll, so they certified the defect (textbook tests-encode-the-bug). FIX: (1) center via table-native text-align+vertical-align, NEVER display:flex on a table cell; (2) removed internal vertical scroll — full height, page scrolls; (3) DESKTOP fit-to-width (`.season-grid-table { table-layout: fixed; width: 100% }`, `.season-grid-scroll { overflow: visible }`) so the header's position:sticky resolves against the app `main.flex-1` scroller and the WK header FLOATS/pins on scroll; team column sticky-left; MOBILE (`@media max-width:640px`) keeps fixed-width table + overflow-x:auto horizontal scroll; (4) rewrote the 4 bug-encoding tests with `not.toMatch(/display:flex/)` tripwires. LESSONS: (a) never `display:flex` a `<th>`/`<td>` — use text-align+vertical-align; (b) "no internal scrollbar + sticky header + wide table" conflict is resolved by fit-to-width (no overflow container) so sticky resolves against the app's own scroller, not an inner box; (c) a CSS comment containing the literal "display:flex" trips a `/display:\s*flex/` test regex — word it "flexbox" instead. GOTCHA update: this session the preview browser DID report a real innerHeight (720), so live scroll/sticky WAS testable (unlike prior sessions' innerHeight=0); screenshot tool still times out on the dense grid → verify via DOM/computed-style.

FOLLOW-UP (2026-07-18, commit `3615b972`): founder happy with layout. Two more tweaks: (1) win%-intensity ramp recolored purple→GREEN (`--sp-shade-1..5` now `rgba(34,160,84, 0.08/0.18/0.30/0.48/0.72)`; darker green = higher win%; white text AA-verified shade-5 5.32:1; NOT the --sp-success token, stays a factual heatmap per claim boundary). (2) First row (WK headers) + first column (team chips) were rendering LEFT-aligned — ROOT CAUSE: shadcn `TableHead` hardcodes `text-left` (client/src/components/ui/table.tsx:76), a Tailwind utility in a CSS layer that BEATS our `@layer components` text-align:center regardless of specificity. FIX: add `text-center` to those TableHead classNames — `cn()` uses twMerge which drops the base text-left. LESSON: to override a shadcn/Tailwind utility class from our own CSS you can't win on specificity (layer order); pass the competing Tailwind utility in className and let twMerge dedupe, or use inline style. Added a rendered-DOM tripwire (className contains text-center, not text-left).

FOLLOW-UP 2 (2026-07-18, commit `4d8b526a`): (1) win% shading is now a DIVERGING scale: <40% red / 40-49% light red / 50-59% NO shading (transparent) / 60-74% light green / 75%+ green (`--sp-shade-1..5`). Same 5 buckets, only colors changed; bucket-3 transparent = "no shading". White cell text AA-verified (red 10:1, green 6.2:1). Still dedicated hues (not danger/success tokens) → factual win% heatmap, claim boundary intact. a11y caveat noted in code: red↔green is least colorblind-safe (win% recoverable via tooltip + spread). (2) Rows shortened ~64px→36px (12 rows above fold vs 7). ROOT CAUSE the rows were stuck tall: shadcn `TableCell`/`TableHead` ship `p-4`/`h-12` Tailwind utilities (client/src/components/ui/table.tsx) that beat our `@layer components` padding/height — SAME layer-precedence problem as the earlier text-left. SYSTEMIC FIX: moved the ENTIRE `.season-grid-*` block OUT of `@layer components` (closed the layer before it; it was the last thing in the layer). **KEY LESSON: unlayered CSS beats ALL @layer rules incl. Tailwind utilities regardless of specificity — when your component CSS must override shadcn/Tailwind utility classes on many properties, unlayer that CSS block rather than fighting per-property with !important or inline styles.** (3) BYE cells: light-grey fill `#d8dce3` + near-black bold text (14.5:1), legend BYE swatch matches. GOTCHA: editing index.css to close the layer left an intermediate extra `}` that put vite/HMR into a stuck CSS-error state; `npm run build` confirmed the file was actually valid, and RESTARTING the dev server (not just reload) cleared the stuck vite state.

FOLLOW-UP 3 (2026-07-18, commit `e4926af1`): founder reverted the diverging red/green (said "step back"). Shipped: (1) SQUARE cells — capped the fit-to-width table (`max-width:1026px`) so 18 week cols land ~52px, paired with 52px row height → 52x52 squares at desktop width (verified at 1500px viewport); below the cap cells go a touch taller-than-wide (no h-scroll, sticky preserved). Chip 34x21, spread 12px. NOTE the cap means the grid no longer fills ultra-wide screens (empty right) — the necessary tradeoff for "a little taller + square" (true fit-to-width squares would be ~78px rows = not "a little"). (2) GREEN shading only for 50%+: buckets 1-2 (<50%) transparent/unshaded; 3=light green (0.22), 4=green (0.42), 5=dark green (0.66). Legend = 3 green bands + "<50%" unshaded.
**DEFERRED — #3 "hide spreads for weeks w/o real lines, N/A rank/win%, no shade": BLOCKED on the wrong-host trap.** Direct query of ep-flat-rice (local dev DB): ALL 272 2026 regular-season games have `spread_used_for_analysis_home=NULL` + only `imported_home_spread` set (early wks real-looking values, late wks ±1.5 placeholders) with IDENTICAL null provenance (spread_source/finality_tier/odds_source all null) — so "real vs placeholder" is NOT distinguishable locally, and the deployed dev app reads **helium** (different data, likely real near-term spreads w/ finality tiers). Can't verify locally. Reverted the speculative backend filter (source!=='missing' — didn't catch imports, which resolve via 'imported_spread_snapshot'). Founder chose "has a finality tier". IMPLEMENTED (commit `6fa50930`): range endpoint `include=analysis` nulls resolvedSpread/winProb unless `game.spreadFinalityTier != null` → placeholder-spread games render scheduled-only (opponent only, no shade, tooltip Rank:N/A + Win%:N/A + Field%). Integration test proves the gate (imported value + no tier → null; RED without gate = "expected -1.5 to be null"). CANNOT verify visually locally (ep-flat-rice has ZERO 2026 games with a finality tier — all imported placeholders → locally the grid shows NO 2026 spreads at all); needs helium visual verification. The finality-tier gate was WRONG (founder: it hid real wk1-5 spreads). REPLACED (commit `604bd4d2`) with the SST-868 fabricated-data rule — the SAME rule the Game Plan "Current season" gate uses (founder wants consistency). Extracted `isFabricatedMagnitudeSet` from `server/services/strategyEngine/ForwardAdapter.ts` (3+-game exact-identity OR 8+-game >=50% modal |spread|-magnitude share; constants MIN_GAMES_FOR_FABRICATION_CHECK=3, MIN_GAMES_FOR_SHARE_CHECK=8, FABRICATION_SHARE_THRESHOLD=0.5) and had `hasCompleteForwardData` delegate to it (behavior-identical, 29 completeness tests still green). Range endpoint (include=analysis) now hides an ENTIRE week whose spreads are fabricated. KEY UNBLOCK: 2026 wk1 4for4 real spreads ARE already in ep-flat-rice (8 distinct mags, 0.25 modal → real/shown); wks 2-18 all ±1.5 (modal 1.0 → fabricated/hidden) — so the rule IS verifiable locally now (wk1 shows, wks 2-18 no-line). Confirmed live. Weeks 2-5 real Odds API spreads are helium-only (founder verifies there). Integration test proves gate (fabricated 3-game wk nulled; real varied wk shown; RED without gate = "expected -1.5 to be null").

UI FOLLOW-UP 4 (same commit): reverted squares → FULL-WIDTH RECTANGLES (removed the max-width:1026 cap; columns fill container via table-layout:fixed width:100%; row height 40px; verified wide rectangles 63x43 at 1600px). Softer/transparent GREEN (rgba(88,186,138, 0.12/0.24/0.40) — SurvivorAtlas-style). BYE cells: removed the light-grey fill + black text → no fill, standard grey label var(--sp-text-quaternary) (same as week-header row); legend BYE swatch back to transparent.

FOLLOW-UP 5 (2026-07-18/19): (a) MOBILE TOOLTIP FIX (commit `98c30ed8`): the Rank/Win%/Field% rollover used a Radix Tooltip (hover/focus only — closes on pointerdown → broken on touch). Replaced with a Radix Popover via a new `SeasonGridCellDetail` component: opens on HOVER for mouse (onPointerEnter pointerType==='mouse'), TAP for touch (PopoverTrigger click), + keyboard focus; Radix handles outside/Escape dismiss; each cell owns its open state (no 576-re-render); trigger stays a plain focusable div (info-only, claim boundary intact). New `.season-grid-cell-popover` CSS sizes it. Verified live: tap opens with correct content, portals out. GOTCHA: synthetic pointer events in the in-app browser can't validate Radix hover (React derives onPointerEnter from pointerover) or Radix outside-dismiss (needs real pointer events) — only tap-open + focus-open are synthetically testable; verify hover/dismiss on a real device. Holiday header markers keep the plain Tooltip. (b) VALIDATION (founder asked): grid IS correct across seasons+archetypes given data — 2021-2025 have real canonical spreads for ALL 272 games (ranks all 18 weeks, every archetype, archetype changes ordering e.g. 2025 wk1 safe_chalk DEN>PHI vs max_equity PHI>DEN); 2026 shows only wk1 (real) + ranks wk1, wks 2-18 fabricated/no-line. (c) Ran /merge-ready (Pre-Publish Gate) + e2e Playwright CI on 2026-v1 @ 98c30ed8 to confirm epic completeness. Epic KEPT OPEN (founder wants it open until launch), NOT marked Complete.

Key files: `client/src/pages/season-grid.tsx`, `client/src/lib/seasonGridData.ts`, `seasonGridShading.ts`, `seasonGridHolidays.ts`, `client/src/index.css` (`.season-grid-*`, `--sp-shade-*`). Internal scrollbar = `.season-grid-scroll { max-height:70vh; overflow-y:auto }`.
