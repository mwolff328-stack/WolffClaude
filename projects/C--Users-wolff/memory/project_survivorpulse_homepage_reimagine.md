---
name: survivorpulse-homepage-reimagine
description: "Homepage Reimagine + Season Schedule Grid for beta launch — founder-approved 4-state design, ships with Game Plan (SE-88); scoping docs + status"
metadata: 
  node_type: memory
  type: project
  originSessionId: e067ddbc-8318-451d-9a95-bddc8f4ee4ea
---

Founder approved (2026-07-15) the reimagined in-app Homepage for the founding-member beta launch. Scoping doc: https://app.notion.com/p/39e29ce5833d81809a83d6f9583dfbd8 (Product & Engineering). Companion: Season Schedule Grid scoping doc https://app.notion.com/p/39e29ce5833d81ba8cbbe7e3b3c21d13.

Key decisions:
- 4-state design, one API call (`GET /api/me/season-debrief/current`), one primary CTA per state: (1) zero pools → Add first pool; (2) picks pending → cockpit strip + CTA to `/game-plan`; (3) all set → calm, no engagement hooks; (4) eliminated everywhere → honest close-out.
- "The Call" renamed **Game Plan** ([[survivorpulse-game-plan]], epic SE-88, stories SST-808..833). Sidenav: My Picks REMOVED, replaced by Game Plan; Set Strategy hidden too. Homepage ships as one package with Game Plan, end of July.
- Jobs amendments applied: no disabled/placeholder UI ever (methodology link ships REAL — Sky writes one-paragraph "How we make the call" page); honesty note is one sentence on Home (full version lives in Game Plan); fight for real kickoff timestamp (Felix adds to season-debrief endpoint if missing); State 4 "recap" copy must not overpromise the My Pools history tab.
- Season Schedule Grid (SurvivorAtlas MAP-style, 32 teams × 18 weeks): separate tool at `/tools/season-grid` via shared TOOLS_NAV + homepage hub card. Phase 1 static = beta (end of July): each cell = opponent + home/away + spread + win%, shaded by win% (spread from canonical games.spreadUsedForAnalysisHome, win prob via existing spread→winprob conversion), Thanksgiving/Christmas header markers. Phase 2 interactive = GA Aug 15: explore/overlay/filters/sort/search + AVAIL/FSV-style columns (Curie-safe definitions). ⚠️ Open founder ruling: SurvivorAtlas's MY MAPS click-to-plan/save/share layer overlaps Game Plan Season View — recommendation is explore+overlay only, with pick assignment routing INTO Game Plan, never a second planner surface.
- Claim boundaries bind everywhere: no Composite Score, no EV-lift, no profit/edge claims ([[survivorpulse-whitepaper-retraction]]).

**Build reality:** 2026-v1 has NO @/components/ds (design-system branch only) — use shadcn ui/ + var(--sp-*) tokens. Data hooks all exist (useCurrentSeasonDebrief is the hub feed; currentWeekPick===null = pending signal; hasData===false = first-run). Deb's mockup: scratchpad homepage-mockup.html (session e067ddbc).

**Grooming complete 2026-07-15** — epic SE-89 (https://app.notion.com/p/39e29ce5833d8162bf93ff0603df39d6), stories SST-835..843: Ann Desc/AC (+isPreSeason = suppression flag not a 5th state; State-4 routes to /pools list w/ Eliminated filter), Vlad 100+ test cases (verified My Pools history NOT recap-worthy → Version B "Review your season" copy ships, "recap" banned), Deb UI specs (new 5-bucket brand-opacity win% shading ramp --sp-shade-1..5 — deliberately NOT the red/green winProbColor pattern, which would read as pick advice; tokens doc needs updating post-build), Sky copy incl. live methodology paragraph, Curie audit PASS (2 required edits applied: no week-level lean detector claim; five-season floor not "ten seasons"). Felix verify: SST-839 = trivially derivable (storage.getGames already kickoff-ordered; mirror schedulerService isPlayoffs→scheduleType mapping); SST-842 = extend existing /api/games/range (close its missing-auth + raw-rows gaps; win prob server-side via shared spreadToWinProbability); 2026 schedule (272 games/18wks) verified present — but in the NEON INTEGRATION DB (.env), helium (real dev, Replit-internal) unverified → check via Replit console before Season Grid build. Build sequencing: homepage State-2/3 CTAs depend on SST-827 (SE-88) landing /game-plan route first.

**BUILD STATUS (2026-07-15): 7 of 9 stories SHIPPED on 2026-v1, all pushed + Done in Notion.** SST-835 df424154 (resolver+container), SST-839 5187a300 (currentWeekKickoffAt on season-debrief, client DTO mirrored), SST-836 10c50849 (cockpit strip + 4-variant CTA card, drift-safe useKickoffCountdown), SST-837 485d4360 (UVP block), SST-840 424f9596 (/methodology page — ProtectedRoute NOT SubscribedShell so the honesty-note link never paywalls), SST-838 aa23d55a (honesty note + hub links; Season Grid card behind SEASON_GRID_LIVE=false flag in homeHubLinksConfig.ts — one-flag flip when SST-843 ships), SST-841 392beda6 (HomeEliminatedStrip; Version B copy behind historyTabRecapVerified=false flag; pools.tsx now parses ?statusFilter=). Then ee120b91: GAME_PLAN_ROUTE flipped /picks→/game-plan (SST-827/SE-88 landed the route in the parallel Game Plan session) + Sky's audited "Add your first pool" CTA label. Visual verification done via dev-auto-login + in-app browser vs ep-blue-tree fixtures: state-1 + /methodology render correctly, 0px horizontal overflow at 375px. Copy constants: homeCopy.ts + homePrimaryActionCopy.ts (state 1 headline/body + states 2/3 strings still TODO(Sky) placeholders — Sky owes a CTA-card copy pass).

**EPIC COMPLETE 2026-07-15 — all 9 stories Done + verified live.** Final wave: SST-842 be7641c8 (extended /api/games/range: requireCustomAuth added, sanitize universal, opt-in ?include=analysis → per-game resolvedSpread/resolvedWinProbability via oddsApply resolveUnifiedEffectiveSpread + shared spreadToWinProbability, nulls never fabricated, byes = absence; existing callers regression-proven byte-identical); SST-843 7ca364f9 (/tools/season-grid: 32×18 matrix, pure libs seasonGridData/Shading/Holidays, --sp-shade-1..5 ramp with measured 8.25:1 bucket-5 contrast, US-Eastern date-derived holiday markers, native overflow NOT Radix ScrollArea — ScrollArea fights position:sticky; SEASON_GRID_LIVE flipped, TOOLS_NAV 5th tool); a8dcde28 Sky final CTA copy + founder no-em-dash ruling (sentence structures only, word-hyphens fine); e6c3fac0 subtitle em-dash fix. Live-verified vs ep-flat-rice (has 2026 schedule; demo@survivorpulse.test = State-2 user): grid renders real matchup/spread/win% cells + bye gaps, internal scroll with sticky team column at 375px, homepage State-2 shows new copy + 3 hub cards + live methodology link, no em-dashes, 0px overflow. FOUNDER RULINGS RECORDED: Phase-2 grid = explore+overlay only, pick actions route into Game Plan; full planner-on-grid = separate future Game Plan view (Pam to scope); no em-dashes in product copy.

**SST-872 (Bug, Done 2026-07-16):** Founder saw Home cockpit "10 entries/3 pools" vs My Pools "20 pools/25 entries" and thought Home was broken. ROOT CAUSE: founder is an ADMIN; GET /api/pools has an admin branch (routes.ts:3902) that returns getAllPoolsWithEntryCountsAndUsername() = EVERY pool in the system when role=ADMIN and no ?userId=. Home debrief correctly uses getPoolsWithEntryCounts(userId)=own createdBy pools. So Home was CORRECT (helium SQL: created=3, entries-in=3, entries=10, all-system=20); My Pools was showing all-system for the admin. Non-admins unaffected (already own-scoped). FIX (founder chose "personal default + admin toggle"): /api/pools + /api/my-pools/historical-data-summary now default to caller's own pools for ALL roles; admins opt into system-wide via `?scope=all` (non-admin passing it = ignored, no leak); ?userId= view-as unchanged + precedence over scope. Commits 6f762fed (Felix backend, 8 integration tests incl. the own-by-default guard) + 3a032cd3 (Deb admin-only View-all-pools toggle, default off, 5 tests). Lesson: when a personal page's numbers look wrong for the FOUNDER, check if it's the admin all-pools path before assuming a data bug — helium isn't reachable locally so confirm role/counts via a founder-run SQL query. Also: ep-flat-rice admins own 0 pools + only 2 pools total for 2026, so it can't reproduce the divergence — temporarily promote a test user to ADMIN (revert after) for a live admin smoke.

**Residual (non-blocking):** helium parity SQL still a founder deploy-time step (SST-842 comment has the query; loader script backfills if short); states 3/4 visual pass unit-tested only; PortfolioLandingSection.tsx:181 has a pre-existing em-dash outside SE-89 scope (app-wide copy sweep candidate); Felix's dual-spread-resolver CLAUDE.md contradiction chipped as a task; Vlad post-deploy smoke: 4 games/range caller surfaces + unauthenticated curl → 401.
